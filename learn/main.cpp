#include <stdio.h>
#include <iostream>
#include "new.h"

int main(){
int a,b;
  int c,d;
std::cin>>a>>b;
  std::cin>>c>>d;
std::cout<<"The result = "<<add(a,b)<<std::endl;
std::cout<<"The Diff = "<<sub(c,d)<<std::endl;
return 0;
}
